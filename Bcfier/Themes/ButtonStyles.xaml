<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:system="clr-namespace:System;assembly=mscorlib"
  xmlns:themes="clr-namespace:Bcfier.Themes"
  mc:Ignorable="d">
  <ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="Icons.xaml" />
    <ResourceDictionary Source="Colors.xaml" />
  </ResourceDictionary.MergedDictionaries>
  <!--
    Stiles used by different buttons and controls
    not all of these are in use
  -->
  <Style x:Key="MainMenuButton" TargetType="themes:ButtonIcon">
    <Style.Setters>
      <Setter Property="IconColor" Value="{StaticResource PrimaryDark}" />
      <Setter Property="HighlightColor" Value="{StaticResource SecondaryLight}" />
      <Setter Property="IsVertical" Value="True" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="DisabledIconColor" Value="{StaticResource SecondaryDark}" />
      <Setter Property="Foreground" Value="{StaticResource BackgroundDark}" />
    </Style.Setters>
  </Style>

  <Style
    x:Key="SaveAsButtonStyle"
    BasedOn="{StaticResource MainMenuButton}"
    TargetType="themes:ButtonIcon">
    <Style.Triggers>
      <DataTrigger Binding="{Binding ElementName=BcfTabControl, Path=SelectedIndex}" Value="-1">
        <Setter Property="IsEnabled" Value="False" />
      </DataTrigger>
    </Style.Triggers>
  </Style>
  
  <Style
    x:Key="MergeButtonStyle"
    BasedOn="{StaticResource SaveAsButtonStyle}"
    TargetType="themes:ButtonIcon" />
  
  <Style x:Key="ImageNullSource" TargetType="Image">
    <Style.Triggers>
      <Trigger Property="Source" Value="{x:Null}">
        <Setter Property="Visibility" Value="Collapsed" />
      </Trigger>
    </Style.Triggers>
  </Style>
  
  <Style
    x:Key="AddViewCommentStyle"
    TargetType="Button">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="BorderBrush" Value="{StaticResource BackgroundLight}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border
                            x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Height="21"           
                                Margin="{TemplateBinding Padding}" />
          </Border>
          <!-- Триггеры для состояний -->
          <ControlTemplate.Triggers>
            <!-- Наведение курсора -->
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="border" Property="Background" Value="{StaticResource SecondaryLight}" />
            </Trigger>
            <!-- Нажатие кнопки -->
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="border" Property="Background" Value="{StaticResource SecondaryLight}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  
  <Style
    x:Key="AddCommentEnablerStyle"
    BasedOn="{StaticResource AddViewCommentStyle}"
    TargetType="Button">
    <Style.Triggers>
      <DataTrigger Binding="{Binding ElementName=IssueList, Path=SelectedItems.Count}" Value="0">
        <Setter Property="IsEnabled" Value="False" />
      </DataTrigger>
      <DataTrigger Binding="{Binding ElementName=TextBoxComment, Path=Text}" Value="{x:Static system:String.Empty}">
        <Setter Property="IsEnabled" Value="False" />
      </DataTrigger>
    </Style.Triggers>
  </Style>

  <Style x:Key="OverlayButton" TargetType="themes:ButtonIcon">
    <Style.Setters>
      <Setter Property="IsVertical" Value="True" />
      <Setter Property="IconMarginVertical" Value="0" />
      <Setter Property="Margin" Value="0" />
      <Setter Property="Padding" Value="0" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="IconSizeVertical" Value="12" />
      <Setter Property="VerticalAlignment" Value="Top" />
      <Setter Property="IconColor" Value="DarkGray" />
      <Setter Property="HorizontalAlignment" Value="Right" />
      <Setter Property="Visibility" Value="Collapsed" />
    </Style.Setters>
    <Style.Triggers>
      <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Grid}, Path=IsMouseOver}" Value="True">
        <Setter Property="Visibility" Value="Visible" />
      </DataTrigger>
    </Style.Triggers>
  </Style>

  <Style
    x:Key="CloseButton"
    BasedOn="{StaticResource OverlayButton}"
    TargetType="themes:ButtonIcon">
    <Style.Setters>
      <Setter Property="IconPath" Value="{StaticResource IconXClose}" />
    </Style.Setters>
  </Style>


  <Style
    x:Key="SaveButtonStyle"
    BasedOn="{StaticResource MainMenuButton}"
    TargetType="themes:ButtonIcon">
    <Setter Property="IsEnabled" Value="False" />
    <Style.Triggers>
      <!-- Enable when there is a selected tab and the file has unsaved changes -->
      <MultiDataTrigger>
        <MultiDataTrigger.Conditions>
          <Condition Binding="{Binding ElementName=BcfTabControl, Path=SelectedItem.HasBeenSaved}" Value="False" />
        </MultiDataTrigger.Conditions>
        <Setter Property="IsEnabled" Value="True" />
      </MultiDataTrigger>
      <!-- Enable when file has never been saved yet (no Fullname) -->
      <DataTrigger Binding="{Binding ElementName=BcfTabControl, Path=SelectedItem.Fullname}" Value="{x:Null}">
        <Setter Property="IsEnabled" Value="True" />
      </DataTrigger>
      <DataTrigger Binding="{Binding ElementName=BcfTabControl, Path=SelectedItem.Fullname}" Value="">
        <Setter Property="IsEnabled" Value="True" />
      </DataTrigger>
      <!-- Disable when no tab is selected -->
      <DataTrigger Binding="{Binding ElementName=BcfTabControl, Path=SelectedIndex}" Value="-1">
        <Setter Property="IsEnabled" Value="False" />
      </DataTrigger>
    </Style.Triggers>
  </Style>
</ResourceDictionary>